export interface IMap {
    mapId: number;
    mapName: string;
    mapDescription?: string;
    mapImage?: string;
}

export interface MapDto {
    mapId: number;
    mapName: string;
    mapDescription?: string;
    mapImage?: string;
}
